{
  "name": "pykachu-go",
  "version": "3.0.0",
  "private": true,
  "description": "Pykachu Go - decode the code. capture pikachu.",
  "scripts": {
    "build": "tailwindcss -i css/tailwind-input.css -o css/tailwind.css --minify",
    "serve": "npx serve -s ."
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "tailwindcss": "^3.4.0"
  }
}
